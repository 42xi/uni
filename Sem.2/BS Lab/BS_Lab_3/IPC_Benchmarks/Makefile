# Makefile to build the various Benchmarks
#
# SYNOPSIS:
#
#   make [all]  - makes everything.
#   make clean  - removes all files generated by make.
#
# Author: Rainer Keller, HS-Esslingen
#

# Added bench_pipe.c to the SOURCES
SOURCES=bench_rdtsc.c bench_signal.c bench_mmap.c bench_pipe.c
ifeq ($(OS),Linux)
    SOURCES+=bench_process_vm_readv.c
endif

BENCHMARKS=$(SOURCES:.c=)

CC=gcc
CFLAGS=-Wall -O2

all: $(BENCHMARKS)

clean:
	rm -f $(BENCHMARKS)

.c:
	$(CC) $(CFLAGS) -o $@ $<
